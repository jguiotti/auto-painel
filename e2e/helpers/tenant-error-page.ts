/** Public hero title on `/erro/concessionaria` (painel + vitrine) — keep in sync with apps. */
export const TENANT_ERROR_HEADING_RE = /Loja não encontrada/i;

/** CTA on store-not-found page — links to marketing home. */
export const STORE_NOT_FOUND_CTA_RE = /Ir para a AutoPainel/i;
